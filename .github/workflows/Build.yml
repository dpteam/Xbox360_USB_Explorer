name: Build Xbox360_USB_Explorer

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v6
      with:
        fetch-depth: 0

    # Add MSBuild to the PATH: https://github.com/microsoft/setup-msbuild
    - name: Setup MSBuild.exe
      uses: microsoft/setup-msbuild@v2

    # Restore the application to populate the obj folder with RuntimeIdentifiers
    - name: Restore the application
      run: msbuild Xbox360_USB_Explorer.slnx /t:Restore /p:Configuration=Release

    # Build the application
    - name: Build the application
      run: msbuild Xbox360_USB_Explorer.slnx /p:Configuration=Release /p:Platform="Any CPU"

    # Upload the release build artifacts
    - name: Upload artifacts
      uses: actions/upload-artifact@v6
      with:
        name: Xbox360_USB_Explorer-Release
        path: D:/a/Xbox360_USB_Explorer/Xbox360_USB_Explorer/bin/Release/net48/Xbox360_USB_Explorer.exe
